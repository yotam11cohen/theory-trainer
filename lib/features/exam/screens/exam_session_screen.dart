import 'package:flutter/material.dart';

class ExamSessionScreen extends StatelessWidget {
  const ExamSessionScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Exam Session')));
}
