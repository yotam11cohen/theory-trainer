name: cleared_driving
description: Israeli Driving Theory — Cleared
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  supabase_flutter: ^2.5.0
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  go_router: ^14.2.0
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  google_sign_in: ^6.2.1
  flutter_local_notifications: ^17.2.2
  flutter_tts: ^4.0.2
  cached_network_image: ^3.3.1
  google_fonts: ^6.2.1
  intl: ^0.20.2
  connectivity_plus: ^6.0.3
  path_provider: ^2.1.3
  timezone: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  riverpod_generator: ^2.4.0
  build_runner: ^2.4.11
  hive_generator: ^2.0.1
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  generate: true
